{
  "name": "feeder-server",
  "version": "1.0.0",
  "description": "Runs on a cloud VM, listens to Apache Kafka, and sends requests to FeederClient to feed fish",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ben Lorantfy",
  "license": "MIT",
  "dependencies": {
    "express": "^4.15.3",
    "socket.io": "^2.0.3"
  }
}
